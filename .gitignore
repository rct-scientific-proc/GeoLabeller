# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# NOT build/ - this repository's build/ holds source: the packaging scripts and
# the WiX installer authoring. Ignoring it wholesale hid new files there from
# git status entirely. Only what a build writes underneath it is ignored.
/build/build/
/build/.build_venv/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
env/
venv/
.venv/
ENV/
env/
deps/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project specific
*.tif
*.tiff

# Test scripts
tests/
todo/
test_data/

# Temp files (e.g. the .tif.tmp written during optimized/mosaic export)
*.tmp

# Scratch for building docs/ (page renders, drafts, generator)
docs_workspace/
